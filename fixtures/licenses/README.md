# Fixture license references

Place the complete reviewed license text for any `LicenseRef-*` expression here,
then set that fixture's `license_file` to this corpus-relative path. Prefer a
standard SPDX expression when one accurately describes the grant. Adding a
license text does not itself establish redistribution permission; the manifest
must still record the reviewed `redistribution` decision and upstream source.

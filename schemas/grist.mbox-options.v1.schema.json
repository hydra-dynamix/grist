{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "EmailOptions": {
      "additionalProperties": false,
      "properties": {
        "inline_attachment_bytes": {
          "description": "Capture decoded attachment bytes inline. Otherwise exact identities and source locators are retained without duplicating source bytes.",
          "type": "boolean"
        },
        "max_decoded_part_bytes": {
          "description": "Maximum decoded bytes retained or recursively parsed for one MIME entity.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_header_bytes": {
          "description": "Maximum RFC 5322 header block accepted as structured headers.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_mime_depth": {
          "description": "Maximum MIME entity depth, with the root entity at depth zero.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "max_mime_parts": {
          "description": "Maximum number of MIME entities emitted for one message.",
          "format": "uint",
          "minimum": 0.0,
          "type": "integer"
        },
        "parse_nested_attachments": {
          "description": "Dispatch locally supplied attachment bytes to enabled Grist parsers.",
          "type": "boolean"
        }
      },
      "required": [
        "inline_attachment_bytes",
        "max_decoded_part_bytes",
        "max_header_bytes",
        "max_mime_depth",
        "max_mime_parts",
        "parse_nested_attachments"
      ],
      "type": "object"
    }
  },
  "properties": {
    "email": {
      "allOf": [
        {
          "$ref": "#/definitions/EmailOptions"
        }
      ],
      "description": "RFC 5322 and MIME limits shared by every message in the mailbox."
    },
    "honor_content_length": {
      "description": "Use a valid Content-Length header to protect unescaped body `From ` lines.",
      "type": "boolean"
    },
    "max_messages": {
      "description": "Maximum messages emitted from one mailbox. A limit hit is partial, never EOF.",
      "format": "uint",
      "minimum": 0.0,
      "type": "integer"
    },
    "max_separator_bytes": {
      "description": "Maximum bytes accepted for one envelope separator line.",
      "format": "uint",
      "minimum": 0.0,
      "type": "integer"
    },
    "recover_missing_initial_separator": {
      "description": "Recover bytes before the first valid separator as a malformed message.",
      "type": "boolean"
    },
    "unescape_from_lines": {
      "description": "Remove one quote marker from mboxo/mboxrd `>From ` body lines.",
      "type": "boolean"
    }
  },
  "required": [
    "email",
    "honor_content_length",
    "max_messages",
    "max_separator_bytes",
    "recover_missing_initial_separator",
    "unescape_from_lines"
  ],
  "title": "MboxOptions",
  "type": "object"
}
